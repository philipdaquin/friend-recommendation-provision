---
# - name: Ensure Nginx is at the latest version
#   apt:
#     name: nginx
#     state: latest
# - name: Make sure Nginx is running
#   systemd:
#     state: started
#     name: nginx

# ---
- include: java.yaml
- include: docker.yaml
- include: jenkins.yaml 
